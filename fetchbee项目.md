## fetchbee项目

## 项目业务逻辑（完整版 - 含维权模块）

### 一、用户注册与登录

#### 1.1 用户注册

**前置条件：**

- 用户名不能与已有用户重复

**业务流程：**

- 用户填写用户名、密码
- 系统检查用户名是否已存在，若存在则提示"用户名已存在"
- 密码使用MD5加密存储
- 初始化账户余额为0元
- 账户状态设为正常（1）
- 认证状态默认为未认证（0）
- 角色默认为普通用户（0）

#### 1.2 用户登录

**前置条件：**

- 用户已注册

**业务流程：**

- 用户输入用户名和密码
- 系统验证用户是否存在，不存在则提示"用户不存在"
- 系统验证密码是否正确（MD5加密对比），错误则提示"密码错误"
- 系统检查账户状态，若被禁用（status=0）则提示"账号已被禁用"
- 生成JWT Token并存入Redis（有效期7天）
- 返回Token和用户基本信息

#### 1.3 修改个人信息

**前置条件：**

- 用户已登录

**业务流程：**

- 用户可修改手机号、地址、头像
- 系统验证用户是否存在
- 更新用户信息

#### 1.4 修改密码

**前置条件：**

- 用户已登录

**业务流程：**

- 用户输入旧密码和新密码
- 系统验证旧密码是否正确，错误则提示"旧密码错误"
- 新密码使用MD5加密后更新

#### 1.5 用户登出

**前置条件：**

- 用户已登录

**业务流程：**

- 从Redis中删除Token
- 用户退出登录状态

------

### 二、学生认证流程

#### 2.1 提交学生认证

**前置条件：**

- 用户已登录

**业务流程：**

- 用户上传学生证照片
- 系统将认证状态更新为"待审核"（1）
- 本阶段仅记录认证图片并进入待审核队列
- 等待管理员审核

#### 2.2 管理员审核认证

**前置条件：**

- 操作者必须是管理员（role=1）

**业务流程：**

- 管理员查看待审核认证列表（verificationStatus=1）
- 管理员审核学生证照片
- 选择审核结果：
  - **通过**：认证状态更新为"已认证"（2）
  - **不通过**：认证状态更新为"审核不通过"（3），填写拒绝原因
- 记录审核时间和备注

#### 2.3 查看认证状态

**前置条件：**

- 用户已登录

**业务流程：**

- 用户查看自己的认证状态
- 显示认证状态、学生证照片、审核时间、审核备注

------

### 三、发布订单流程

#### 3.1 发布订单

**前置条件：**

- 用户已登录
- **必须完成学生认证（verificationStatus=2）**，否则提示"请先学生认证"
- **余额必须充足**，否则提示"余额不足，当前余额：X元"

**参数验证：**

- 截止时间必须晚于当前时间
- 截止时间必须至少在15分钟之后
- 截止时间不能超过30天

**业务流程：**

- 用户填写订单信息：
  - 快递公司
  - 取件码
  - 快递描述
  - 取件地址
  - 截止时间
  - 报酬金额
- 系统生成订单号（格式：FO+时间戳+随机码）
- 送达地址自动使用发布者的地址
- 订单状态设为"待审核"（0）
- **从发布者余额中扣除报酬金额**（资金冻结）
- 记录余额变动（类型：订单扣款）
- 订单进入管理员待审核列表

#### 3.2 管理员审核订单

**前置条件：**

- 操作者必须是管理员（role=1）
- 订单状态必须是"待审核"（0）

**业务流程：**

- 管理员查看待审核订单列表
- 管理员审核订单（通过/不通过）：
  - **通过**：审核状态更新为已通过（reviewStatus=1），订单状态变为"待接单"（1），进入订单大厅
  - **不通过**：审核状态更新为已拒绝（reviewStatus=2），订单状态变为"已拒绝"（6），并填写拒绝原因
- 若审核不通过，系统自动将冻结金额全额退回发布者
- **发送通知给发布者**："订单【订单号】审核通过/未通过"

------

### 四、接单配送流程

#### 4.1 浏览待接单列表

**前置条件：**

- 用户已登录

**业务流程：**

- 显示所有待接单状态的订单
- **取件码对非相关用户隐藏（显示为"***"）**
- 显示订单详情：快递公司、取件地址、送达地址、报酬、截止时间

#### 4.2 接单

**前置条件：**

- 用户已登录
- **必须完成学生认证（verificationStatus=2）**，否则提示"请先学生认证"

**业务规则：**

- 订单必须是"待接单"状态（1），否则提示"该订单已被接单或已完成"
- **不能接自己发布的订单**，否则提示"不能接自己发布的订单"
- **订单不能已过期**（当前时间>截止时间），否则提示"订单已过期，无法接单"

**业务流程：**

- 用户点击接单
- 系统更新订单状态为"已接单"（2）
- 记录接单者ID
- **发送通知给发布者**："您的订单【订单号】已被 XXX 接单"
- 接单者可以看到完整的取件码

#### 4.3 取件配送

**业务流程：**

- 接单者根据取件地址和取件码前往取件
- 接单者将快递送往送达地址

#### 4.4 标记送达

**前置条件：**

- 订单必须是"已接单"状态（2），否则提示"订单状态不正确"
- **只有接单者本人可以操作**，否则提示"只有接单者才能标记送达"

**业务流程：**

- 接单者送达后点击"标记送达"
- 系统更新订单状态为"待确认"（3）
- 记录送达时间
- **发送通知给发布者**："您的快递已送达，请确认收货"

------

### 五、订单确认与结算

#### 5.1 确认收货（发布者操作）

**前置条件：**

- 订单必须是"待确认"状态（3），否则提示"订单状态不正确"
- **只有发布者本人可以操作**，否则提示"只有发布者才能确认收货"

**业务流程：**

- 发布者收到快递后点击"确认收货"
- 系统判断是否超时（当前时间 > 截止时间）：
  - **按时完成**：实际支付 = 报酬金额（100%）
  - **超时完成**：实际支付 = 报酬金额 × 80%
- 系统更新订单状态为"已完成"（4）
- 记录完成时间和实际支付金额
- **转账给接单者**（报酬到账）
- 记录余额变动（类型：订单收入）
- **发送通知给接单者**："订单【订单号】已完成，报酬 ¥X 已到账（超时）"

#### 5.2 订单超时自动确认

**触发条件：**

- 定时任务每小时执行一次
- 订单状态为"待确认"（3）
- 送达时间超过24小时未确认

**业务流程：**

- 系统自动查找超过24小时未确认的订单
- 若订单处于维权中（rightsStatus=1），本次自动确认跳过该订单
- 判断是否超时（当前时间 > 截止时间）：
  - **按时完成**：实际支付 = 报酬金额（100%）
  - **超时完成**：实际支付 = 报酬金额 × 80%
- 系统更新订单状态为"已完成"（4）
- **转账给接单者**
- **发送通知给发布者**："订单【订单号】已自动确认完成"
- **发送通知给接单者**："订单【订单号】已自动确认完成，报酬 ¥X 已到账（超时）"

------

### 六、订单取消与退款

#### 6.1 取消订单（发布者操作）

**前置条件：**

- **只有发布者本人可以取消**，否则提示"只有发布者才能取消订单"
- **只能取消"待接单"状态的订单**，否则提示"该订单无法取消"

**业务流程：**

- 发布者点击取消订单，可选填取消原因
- 系统更新订单状态为"已取消"（5）
- 记录取消原因
- **退款给发布者**（全额退回）
- 记录余额变动（类型：订单退款）

#### 6.2 订单过期自动取消

**触发条件：**

- 定时任务每10分钟执行一次
- 订单状态为"待接单"（1）
- 当前时间 > 截止时间

**业务流程：**

- 系统自动查找过期未接单的订单
- 系统更新订单状态为"已取消"（5）
- 取消原因设为"订单已过期，无人接单"
- **退款给发布者**（全额退回）
- **发送通知给发布者**："订单【订单号】已过期，报酬 ¥X 已退回"

------

### 七、维权模块

#### 7.1 提交维权申请

**前置条件：**

- 用户已登录
- **订单状态必须是"已接单"（2）或"待确认"（3）**，否则提示"当前订单状态不允许申请维权"
- **只有订单发布者或接单者可以申请**，否则提示"只有订单发布者或接单者才能申请维权"
- **不能在已有维权申请审核中时申请**，否则提示"该订单已有维权申请正在审核中，请等待审核结果"
- **每人只能申请一次维权**，否则提示"您已经申请过维权，不能重复申请"

**业务规则：**

- 只允许一方申请，等审核结束后另一方才能申请
- 维权期间订单冻结，不能继续操作（标记送达、确认收货等）

**业务流程：**

- 用户填写维权信息：
  - 维权描述
  - 维权照片凭证
- 系统记录维权申请人（publisher/receiver）
- 系统更新维权状态为"维权中"（1）
- 记录维权申请时间
- **发送通知给对方**："订单【订单号】对方已申请维权，请等待管理员审核"

#### 7.2 查看维权状态

**前置条件：**

- 用户已登录
- **只有订单发布者或接单者可以查看**，否则提示"只有订单发布者或接单者才能查看维权信息"

**业务流程：**

- 用户查看维权状态
- 显示维权信息：申请人、维权描述、照片凭证、申请时间、审核状态、审核备注

#### 7.3 管理员审核维权

**前置条件：**

- 操作者必须是管理员（role=1）
- 订单必须有待审核的维权申请（rightsStatus=1）

**业务流程：**

- 管理员查看待审核维权列表

- 管理员查看维权详情：

  - 订单基本信息（订单号、发布者、接单者、报酬）
  - 维权申请人
  - 维权描述
  - 维权照片凭证

- 管理员选择审核结果：

  - 维权通过（2）

    ：

    - 如果是

      发布者申请维权通过

      ：

      - 订单取消
      - 全额退款给发布者
      - 接单者不得报酬

    - 如果是

      接单者申请维权通过

      ：

      - 管理员必须选择资金流向：
        - 选择"发布者"：订单取消，全额退款给发布者
        - 选择"接单者"：订单取消，全额支付给接单者

  - 维权不通过（3）

    ：

    - 订单继续原流程
    - 可填写审核备注说明原因

- 记录审核时间和备注

- **发送通知给双方**："订单【订单号】维权审核结果：通过/不通过"

#### 7.4 维权资金处理规则

**发布者维权通过：**

- 场景：发布者认为没收到货、货物有问题等
- 处理：订单取消 + 全额退款给发布者
- 资金流向：发布者

**接单者维权通过：**

- 场景A：接单者取不到件
  - 管理员判断：发布者责任
  - 处理：订单取消 + 全额退款给发布者
  - 资金流向：发布者
- 场景B：接单者已送达但发布者恶意不确认
  - 管理员判断：接单者无责
  - 处理：订单取消 + 全额支付给接单者
  - 资金流向：接单者

**维权不通过：**

- 订单继续原流程
- 如果是待确认状态，24小时后自动确认
- 资金最终流向由订单正常流程决定

------

### 八、通知系统

#### 8.1 通知触发场景

- 订单审核完成（通过/不通过）→ 通知发布者
- 订单被接单 → 通知发布者
- 快递已送达 → 通知发布者
- 订单已完成 → 通知接单者
- 订单已过期 → 通知发布者
- 订单自动确认 → 通知发布者和接单者
- **维权申请已提交 → 通知对方**
- **维权审核完成 → 通知双方**

#### 8.2 通知管理

**前置条件：**

- 用户已登录

**业务流程：**

- 查看未读通知列表和数量
- 查看所有通知（包括已读和未读）
- 标记单个通知为已读
- 标记所有通知为已读
- 删除通知

------

### 九、管理功能

#### 9.1 学生认证审核

**前置条件：**

- 操作者必须是管理员（role=1）

**业务流程：**

- 查看待审核认证列表
- 审核学生认证（通过/不通过）
- 填写审核备注

#### 9.2 维权审核

**前置条件：**

- 操作者必须是管理员（role=1）

**业务流程：**

- 查看待审核维权列表
- 审核维权申请（通过/不通过）
- 如果是接单者维权通过，选择资金流向
- 填写审核备注

#### 9.3 订单审核

**前置条件：**

- 操作者必须是管理员（role=1）

**业务流程：**

- 查看待审核订单列表（reviewStatus=0）
- 查看所有订单（管理员视图）
- 审核订单（通过/不通过）
- 审核不通过时必须填写拒绝原因
- 审核不通过时系统自动退款给发布者

#### 9.4 管理员统计数据

**前置条件：**

- 操作者必须是管理员（role=1）

**业务流程：**

- 获取待审核订单数量
- 获取待审核认证数量
- 获取待审核维权数量
- 获取公告总数

------

### 十、隐私保护规则

#### 10.1 取件码隐私保护

- **只有发布者和接单者可以看到完整取件码**
- 其他用户浏览订单时，取件码显示为"***"

------

### 十一、订单状态流转图



```
待审核(0) → 待接单(1) → 已接单(2) → 待确认(3) → 已完成(4)
    ↓          ↓           ↓            ↓
  已拒绝(6)  已取消(5) ← 已取消(5) ← 已取消(5)
                           （维权通过）
```

**状态说明：**

- 0-待审核：订单刚发布，等待管理员审核
- 1-待接单：订单审核通过，等待他人接单
- 2-已接单：已有人接单，正在配送中
- 3-待确认：接单者已标记送达，等待发布者确认
- 4-已完成：发布者确认收货或系统自动确认，订单完成
- 5-已取消：发布者取消、系统自动取消过期订单、或维权通过取消
- 6-已拒绝：管理员审核不通过，订单不进入待接单列表

**维权状态说明：**

- 0-无维权：订单正常流程
- 1-维权中：有维权申请待审核，订单冻结
- 2-维权通过：管理员审核通过，订单取消并处理资金
- 3-维权不通过：管理员审核不通过，订单继续原流程

**订单审核状态说明（reviewStatus）：**

- 0-待审核
- 1-已通过
- 2-已拒绝

------

### 十二、核心业务规则总结

1. **学生认证是核心门槛**：发布订单和接单都必须完成学生认证
2. **订单发布先审核**：发布后先进入待审核，管理员审核通过后才进入订单大厅
3. **资金安全保障**：发布时冻结资金，完成后自动结算，取消或审核拒绝时自动退款
4. **超时惩罚机制**：超时完成只支付80%报酬，激励按时配送
5. **双方确认机制**：接单者标记送达 + 发布者确认收货，防止纠纷
6. **自动化处理**：订单过期自动取消（10分钟检查）、超时自动确认（24小时后）
7. **权限控制严格**：只有发布者能取消/确认，只有接单者能标记送达
8. **隐私保护**：取件码只对相关用户可见
9. **时间限制合理**：截止时间至少15分钟后，最多30天内
10. **防止自接单**：不能接自己发布的订单
11. **实时通知**：订单状态变化和审核结果实时通知相关用户
12. **维权保护机制**：订单纠纷时可申请维权，管理员公正裁决
13. **维权冻结机制**：维权期间订单冻结，防止恶意操作
14. **资金流向灵活**：接单者维权通过时，管理员可根据实际情况选择资金流向
15. **维权申请限制**：每人只能申请一次，且需等待对方维权审核结束



## 公告栏模块

### 1. 管理员创建公告

**接口：** POST /admin/announcements

**前置条件：**

- 用户已登录
- 用户角色为管理员（role = 1）
- 请求参数包含公告标题和内容
- 标题和内容均不能为空

**业务流程：**

1. 从请求中获取当前登录用户ID
2. 调用 checkAdminRole() 验证用户是否为管理员
   - 查询用户信息
   - 检查用户是否存在
   - 检查用户角色是否为管理员（role = 1）
3. 创建公告对象，设置以下属性：
   - 标题：从请求参数获取
   - 内容：从请求参数获取
   - 发布者ID：当前管理员用户ID
   - 状态：默认为1（已发布）
4. 调用 Mapper 插入公告到数据库
5. 记录操作日志

**异常处理：**

- 用户不存在：抛出 USER_NOT_FOUND 异常
- 用户非管理员：抛出 FORBIDDEN 异常
- 标题或内容为空：抛出参数校验异常
- 数据库插入失败：抛出 BusinessException

**后置条件：**

- 公告成功创建并保存到数据库
- 公告状态为已发布（status = 1）
- 返回创建成功消息

------

### 2. 管理员更新公告

**接口：** PUT /admin/announcements/{id}

**前置条件：**

- 用户已登录
- 用户角色为管理员（role = 1）
- 公告ID存在
- 请求参数包含新的标题和内容
- 标题和内容均不能为空

**业务流程：**

1. 从请求中获取当前登录用户ID和公告ID
2. 调用 checkAdminRole() 验证用户是否为管理员
3. 根据公告ID查询公告是否存在
4. 更新公告信息：
   - 标题：更新为新标题
   - 内容：更新为新内容
   - 更新时间：自动更新（数据库触发器）
5. 调用 Mapper 更新公告到数据库
6. 记录操作日志

**异常处理：**

- 用户不存在：抛出 USER_NOT_FOUND 异常
- 用户非管理员：抛出 FORBIDDEN 异常
- 公告不存在：抛出 NOT_FOUND 异常
- 标题或内容为空：抛出参数校验异常
- 数据库更新失败：抛出 BusinessException

**后置条件：**

- 公告信息成功更新
- 更新时间自动刷新
- 返回更新成功消息

------

### 3. 管理员删除公告（逻辑删除）

**接口：** DELETE /admin/announcements/{id}

**前置条件：**

- 用户已登录
- 用户角色为管理员（role = 1）
- 公告ID存在

**业务流程：**

1. 从请求中获取当前登录用户ID和公告ID
2. 调用 checkAdminRole() 验证用户是否为管理员
3. 根据公告ID查询公告是否存在
4. 执行逻辑删除：
   - 将公告状态（status）修改为0（已下架）
   - 不从数据库物理删除记录
5. 调用 Mapper 更新公告状态
6. 记录操作日志

**异常处理：**

- 用户不存在：抛出 USER_NOT_FOUND 异常
- 用户非管理员：抛出 FORBIDDEN 异常
- 公告不存在：抛出 NOT_FOUND 异常
- 数据库更新失败：抛出 BusinessException

**后置条件：**

- 公告状态变为已下架（status = 0）
- 公告记录仍保留在数据库中
- 普通用户无法查看该公告
- 管理员仍可在管理界面看到该公告
- 返回删除成功消息

------

### 4. 管理员查询所有公告

**接口：** GET /admin/announcements

**前置条件：**

- 用户已登录
- 用户角色为管理员（role = 1）

**业务流程：**

1. 从请求中获取当前登录用户ID
2. 调用 checkAdminRole() 验证用户是否为管理员
3. 查询所有公告（包括已发布和已下架的公告）
4. 关联查询发布者用户信息，获取发布者用户名
5. 按创建时间倒序排列
6. 返回公告列表（AnnouncementVO格式）

**异常处理：**

- 用户不存在：抛出 USER_NOT_FOUND 异常
- 用户非管理员：抛出 FORBIDDEN 异常

**后置条件：**

- 返回所有公告列表，包含以下信息：
  - 公告ID
  - 标题
  - 内容
  - 发布者ID
  - 发布者用户名
  - 状态（1-已发布，0-已下架）
  - 创建时间
  - 更新时间

------

### 5. 普通用户查询最新公告

**接口：** GET /announcements/latest

**前置条件：**

- 用户已登录
- 无角色限制（普通用户/管理员均可）

**业务流程：**

1. 查询最新的已发布公告（status = 1）
2. 关联查询发布者用户信息，获取发布者用户名
3. 按创建时间倒序排列，取第一条记录
4. 返回公告详情（AnnouncementVO格式）

**异常处理：**

- 无已发布公告：返回 null 或空对象

**后置条件：**

- 返回最新的已发布公告，包含以下信息：
  - 公告ID
  - 标题
  - 内容
  - 发布者ID
  - 发布者用户名
  - 状态（固定为1-已发布）
  - 创建时间
  - 更新时间
- 如果没有已发布公告，返回空
