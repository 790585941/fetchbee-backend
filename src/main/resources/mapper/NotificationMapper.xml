<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.fetchbeebackend.mapper.NotificationMapper">
    
    <!-- 结果映射 -->
    <resultMap id="NotificationResultMap" type="com.example.fetchbeebackend.entity.Notification">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="type" column="type"/>
        <result property="title" column="title"/>
        <result property="content" column="content"/>
        <result property="orderId" column="order_id"/>
        <result property="isRead" column="is_read"/>
        <result property="createTime" column="create_time"/>
    </resultMap>
    
    <!-- 插入通知 -->
    <insert id="insert" parameterType="com.example.fetchbeebackend.entity.Notification"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO notification (user_id, type, title, content, order_id, is_read)
        VALUES (#{userId}, #{type}, #{title}, #{content}, #{orderId}, #{isRead})
    </insert>
    
    <!-- 根据ID查询通知 -->
    <select id="findById" resultMap="NotificationResultMap">
        SELECT * FROM notification
        WHERE id = #{id}
    </select>
    
    <!-- 查询用户的未读通知列表 -->
    <select id="findUnreadByUserId" resultMap="NotificationResultMap">
        SELECT * FROM notification
        WHERE user_id = #{userId} AND is_read = 0
        ORDER BY create_time DESC
    </select>
    
    <!-- 查询用户的所有通知列表 -->
    <select id="findByUserId" resultMap="NotificationResultMap">
        SELECT * FROM notification
        WHERE user_id = #{userId}
        ORDER BY create_time DESC
    </select>
    
    <!-- 查询用户的未读通知数量 -->
    <select id="countUnreadByUserId" resultType="int">
        SELECT COUNT(*) FROM notification
        WHERE user_id = #{userId} AND is_read = 0
    </select>
    
    <!-- 标记通知为已读 -->
    <update id="markAsRead">
        UPDATE notification
        SET is_read = 1
        WHERE id = #{id}
    </update>
    
    <!-- 标记用户的所有通知为已读 -->
    <update id="markAllAsRead">
        UPDATE notification
        SET is_read = 1
        WHERE user_id = #{userId} AND is_read = 0
    </update>
    
    <!-- 删除通知 -->
    <delete id="deleteById">
        DELETE FROM notification
        WHERE id = #{id}
    </delete>
    
    <!-- 删除用户的所有通知 -->
    <delete id="deleteByUserId">
        DELETE FROM notification
        WHERE user_id = #{userId}
    </delete>
    
</mapper>

